name: 🚀 Deploy Site on GitHub Pages

on:
  push:
    branches:
      - main  # Déclenche le workflow uniquement sur la branche principale

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 📂 Checkout Repository
        uses: actions/checkout@v4

      - name: 📦 Upload GitHub Pages artifact
        uses: actions/upload-pages-artifact@v2
        with:
          path: ./  # Met tous les fichiers du repo dans l'artifact

  deploy:
    runs-on: ubuntu-latest
    needs: build  # Assure que le build est fini avant de déployer
    steps:
      - name: 📄 Setup Pages
        uses: actions/configure-pages@v4

      - name: 🚀 Deploy to GitHub Pages
        uses: actions/deploy-pages@v4